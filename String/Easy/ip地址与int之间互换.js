/**
 *将一个字符串ip转换成一个数字
 *
 * @param {string} ip
 * @return {number}
 */
function ipToNumber(ip) {}

/**
 *将一个数字转换成一个字符串ip地址
 *
 * @param {number} num
 * @return {string} ip
 */
function numberToIp(num) {}
